# Hashes der drei Staging-Quelldateien zum Zeitpunkt der letzten manuellen
# Synchronisierung bei Hostpoint (Drift-Guard, ENT-384/ENT-387). Der
# Staging-Deploy vergleicht diese Werte mit den aktuellen Hashes der drei
# Dateien und bricht bei jeder Abweichung ab -- siehe deploy-hostpoint.yml,
# Schritt "Umgebung waehlen und erforderliche Secrets pruefen".
#
# htaccess-hostpoint       -> unterhalb von "#@__HCP_END__@#" in die
#                             Staging-.htaccess (Passwortschutz-Block bleibt
#                             oberhalb unangetastet)
# htaccess-staging-zusatz  -> direkt im Anschluss daran, in derselben
#                             Staging-.htaccess (X-Robots-Tag, ENT-387)
# robots-staging.txt       -> als eigene Datei robots.txt in www/staging
#
# Aktualisieren nach jeder inhaltlichen Aenderung an einer der drei Dateien:
# 1. sha256sum htaccess-hostpoint htaccess-staging-zusatz robots-staging.txt
# 2. Die geaenderte Datei wie oben beschrieben erneut manuell bei Hostpoint
#    eintragen (Explorer -> www/staging).
# 3. Den neuen Hash unten in der passenden Zeile eintragen und committen.
# ── OFFEN, Stand 2026-09-08 (ENT-451/OP-454 und der Merge von main) ──────
# Der Hash unten gehoert zur ZUSAMMENGEFUEHRTEN Fassung von
# htaccess-hostpoint. Er ist neu berechnet, weil nach dem Merge WEDER der
# Hash dieses Branches NOCH der von main zur Datei passt -- beide Seiten
# hatten je eigene Eintraege ergaenzt.
#
# Zwei Nachtraege bei Hostpoint stehen aus oder sind ungeklaert:
#
#  (a) lohn.php, gavzeit.php und lohnlauf.php (ENT-451). Der Hash wurde
#      mitgezogen, BEVOR der manuelle Nachtrag erledigt war -- vom
#      Projektinhaber am 2026-09-07 so entschieden, festgehalten als OP-454.
#      Der Nachtrag STEHT WEITERHIN AUS.
#
#  (b) demo_anfrage.php, aus main uebernommen. Ob der Eintrag dort bereits
#      manuell bei Hostpoint nachgetragen wurde, ist NICHT dokumentiert --
#      die Fassung auf main traegt keinen Hinweis dazu. Ich habe es weder
#      geprueft noch behaupte ich das eine oder andere; wer den Nachtrag
#      macht, prueft diesen Eintrag mit.
#
#  (c) Permissions-Policy: geolocation von "()" auf "(self)" (ENT-501,
#      2026-09-10). Die leere Liste sperrte den Standort auch fuer die
#      eigene Seite -- app.html braucht ihn an neun Stellen, und am
#      Standort haengt der Alleinarbeiterschutz. Auch dieser Hash ist dem
#      manuellen Nachtrag vorausgeeilt, aus demselben Grund wie (a): Ohne
#      ihn waere die Regression dauerhaft rot und nichts liesse sich mehr
#      schieben. STEHT AUS. Fuer Staging heisst das bis zum Nachtrag: Die
#      Ortung bleibt dort gesperrt, ein Rundgangstest auf Staging kann
#      also keine Position erfassen -- auf Production greift die neue
#      Zeile mit dem naechsten Deploy von selbst.
#
#  (d) betreiber.php in die FilesMatch-Sperre aufgenommen (ENT-519,
#      2026-09-10). Das Modul der Betreiber-Ebene wird nur von den
#      betreiber_*-Endpunkten eingebunden und nie direkt aufgerufen --
#      dieselbe Begruendung wie bei kundenportal.php. Auch dieser Hash ist
#      dem manuellen Nachtrag vorausgeeilt, aus demselben Grund wie (a) und
#      (c). STEHT AUS. Bis zum Nachtrag ist betreiber.php auf Staging per
#      URL erreichbar; die Datei enthaelt ausschliesslich
#      Funktionsdefinitionen und gibt bei direktem Aufruf nichts aus, und
#      Staging liegt zusaetzlich hinter der Basic-Auth-Schicht (ENT-384).
#      Auf Production greift die neue Zeile mit dem naechsten Deploy von
#      selbst.
#
#  (e) support.php in die FilesMatch-Sperre aufgenommen (ENT-526,
#      2026-09-11). Das Modul der Support-Freigabe wird von beiden Seiten
#      eingebunden und nie direkt aufgerufen -- dieselbe Begruendung wie bei
#      kundenportal.php und betreiber.php. Auch dieser Hash ist dem
#      manuellen Nachtrag vorausgeeilt, aus demselben Grund wie (a), (c) und
#      (d). STEHT AUS. Die Datei enthaelt ausschliesslich
#      Funktionsdefinitionen und gibt bei direktem Aufruf nichts aus;
#      Staging liegt zusaetzlich hinter der Basic-Auth-Schicht (ENT-384).
#
#  (f) supportvorgang.php in die FilesMatch-Sperre aufgenommen (ENT-538,
#      2026-09-11). Das Modul der Supportvorgaenge wird von beiden Seiten
#      eingebunden -- vom Cockpit (support_anfrage.php) und vom
#      Betreiber-Bereich (betreiber_support_vorgang.php) -- und nie direkt
#      aufgerufen; dieselbe Begruendung wie bei (d) und (e). Auch dieser
#      Hash ist dem manuellen Nachtrag vorausgeeilt, aus demselben Grund wie
#      (a), (c), (d) und (e). STEHT AUS. Die Datei enthaelt ausschliesslich
#      Funktionsdefinitionen und gibt bei direktem Aufruf nichts aus;
#      Staging liegt zusaetzlich hinter der Basic-Auth-Schicht (ENT-384).
#
#  (g) demo_daten.php in die FilesMatch-Sperre aufgenommen (ENT-523,
#      2026-09-12). Der Musterbetrieb-Erzeugungskern fuer die Demo-Umgebung
#      wird ausschliesslich von api/demo_daten_erzeugen.php eingebunden und
#      nie direkt aufgerufen -- dieselbe Begruendung wie bei (d), (e) und
#      (f). Auch dieser Hash ist dem manuellen Nachtrag vorausgeeilt, aus
#      demselben Grund wie (a), (c), (d), (e) und (f). STEHT AUS. Staging
#      betreibt ohnehin keine Demo-Umgebung -- require_demo_umgebung() in
#      demo_daten_erzeugen.php liefert dort so oder so 404 --, trotzdem
#      enthaelt demo_daten.php selbst wie die anderen Faelle ausschliesslich
#      Funktionsdefinitionen und gibt bei direktem Aufruf nichts aus;
#      Staging liegt zusaetzlich hinter der Basic-Auth-Schicht (ENT-384).
#
#  (h) demo_bremse.php in die FilesMatch-Sperre aufgenommen (2026-09-13).
#      Die dateibasierte Bremse des Demo-Formulars ersetzt dort die
#      MySQL-Anmeldebremse, weil die Homepage auf ihrer eigenen Domain
#      (guardops.ch) ohne Datenbank laeuft. Sie wird nur von
#      api/demo_senden.php eingebunden und nie direkt aufgerufen --
#      dieselbe Begruendung wie bei (d), (e) und (f). Auch dieser Hash ist
#      dem manuellen Nachtrag vorausgeeilt, aus demselben Grund wie (a),
#      (c) bis (g). STEHT AUS. Die Datei enthaelt ausschliesslich
#      Funktionsdefinitionen und gibt bei direktem Aufruf nichts aus;
#      Staging liegt zusaetzlich hinter der Basic-Auth-Schicht (ENT-384).
#
#  (i) demo_reset.php in die FilesMatch-Sperre aufgenommen (ENT-523,
#      2026-09-16). Der Rechenkern des naechtlichen Demo-Resets wird
#      ausschliesslich von api/demo_reset_ausfuehren.php eingebunden und
#      nie direkt aufgerufen -- dieselbe Begruendung wie bei (g), dem sehr
#      aehnlichen demo_daten.php. Auch dieser Hash ist dem manuellen
#      Nachtrag vorausgeeilt, aus demselben Grund wie (a), (c) bis (h).
#      STEHT AUS. Staging betreibt ohnehin keine Demo-Umgebung --
#      require_demo_umgebung() im Endpunkt liefert dort so oder so 404 --,
#      trotzdem enthaelt demo_reset.php selbst wie die anderen Faelle
#      ausschliesslich Funktionsdefinitionen und gibt bei direktem Aufruf
#      nichts aus; Staging liegt zusaetzlich hinter der
#      Basic-Auth-Schicht (ENT-384).
#
#  (j) demo_zugang.php in die FilesMatch-Sperre aufgenommen (ENT-600,
#      2026-09-17). Der Rechenkern der Demo-Zugaenge wird von
#      backend/betreiber.php per require_once eingebunden und nie direkt
#      aufgerufen -- dieselbe Begruendung wie bei (d) bis (i). Auch dieser
#      Hash ist dem manuellen Nachtrag vorausgeeilt, aus demselben Grund wie
#      (a), (c) bis (i). STEHT AUS. Die Datei enthaelt ausschliesslich
#      Funktionsdefinitionen und Konstanten und gibt bei direktem Aufruf
#      nichts aus; Staging liegt zusaetzlich hinter der Basic-Auth-Schicht
#      (ENT-384).
#
#  (k) mail_vorlage.php in die FilesMatch-Sperre aufgenommen (ENT-619,
#      2026-09-18). Die gemeinsame Gestaltung der Mails, die im Namen der
#      Betreiberin hinausgehen; sie wird von backend/demo_zugang.php per
#      require_once eingebunden und nie direkt aufgerufen -- dieselbe
#      Begruendung wie bei (d) bis (j). Auch dieser Hash ist dem manuellen
#      Nachtrag vorausgeeilt, aus demselben Grund wie (a), (c) bis (j):
#      Ohne ihn waere die Regression dauerhaft rot und nichts liesse sich
#      mehr schieben. STEHT AUS. Die Datei enthaelt ausschliesslich
#      Funktionsdefinitionen und Konstanten und gibt bei direktem Aufruf
#      nichts aus; Staging liegt zusaetzlich hinter der Basic-Auth-Schicht
#      (ENT-384).
#
#  (l) demo_instanz.php und planung_einrichten_kern.php in die
#      FilesMatch-Sperre aufgenommen (2026-09-18). Beide sind Rechenkerne,
#      die ausschliesslich per require_once aus backend/api/*.php gezogen
#      und nie direkt aufgerufen werden -- dieselbe Begruendung wie bei (d)
#      bis (k). Sie kommen neu auch ins Rapport-Tool- und ins
#      cupi24-Buendel, weil die Endpunkte, die sie einbinden, dort schon
#      lagen und ohne sie mit HTTP 500 abbrachen. Auch dieser Hash ist dem
#      manuellen Nachtrag vorausgeeilt, aus demselben Grund wie (a), (c) bis
#      (k): Ohne ihn waere die Regression dauerhaft rot und nichts liesse
#      sich mehr schieben. STEHT AUS. Beide Dateien enthalten ausschliesslich
#      Funktionsdefinitionen und geben bei direktem Aufruf nichts aus;
#      Staging liegt zusaetzlich hinter der Basic-Auth-Schicht (ENT-384).
#
#  (m) demo_bestaetigung.php in die FilesMatch-Sperre aufgenommen (ENT-624,
#      2026-09-19). Die offene Demo-Anfrage vor der Bestaetigung: Tabelle,
#      Bestaetigungswert und die Mail mit dem Link. Wird per require_once
#      aus backend/api/demo_anfordern.php und backend/api/demo_bestaetigen.php
#      gezogen und nie direkt aufgerufen -- dieselbe Begruendung wie bei (d)
#      bis (l). Auch dieser Hash ist dem manuellen Nachtrag vorausgeeilt,
#      aus demselben Grund wie (a), (c) bis (l): Ohne ihn waere die
#      Regression dauerhaft rot und nichts liesse sich mehr schieben.
#      STEHT AUS. Die Datei enthaelt ausschliesslich Funktionsdefinitionen
#      und Konstanten und gibt bei direktem Aufruf nichts aus; Staging
#      liegt zusaetzlich hinter der Basic-Auth-Schicht (ENT-384).
#
#  (n) tutorial.php in die FilesMatch-Sperre aufgenommen (2026-09-21).
#      Haelt fest, wer die Cockpit-Tour und den Demo-Hinweis schon gesehen
#      hat; wird per require_once aus backend/api/login.php und
#      backend/api/me.php gezogen und nie direkt aufgerufen -- dieselbe
#      Begruendung wie bei (l). Kommt aus demselben Grund neu auch ins
#      Rapport-Tool- und ins cupi24-Buendel. Auch dieser Hash ist dem
#      manuellen Nachtrag vorausgeeilt, aus demselben Grund wie (a), (c)
#      bis (m): Ohne ihn waere die Regression dauerhaft rot und nichts
#      liesse sich mehr schieben. STEHT AUS. Die Datei enthaelt
#      ausschliesslich Funktionsdefinitionen und gibt bei direktem Aufruf
#      nichts aus; Staging liegt zusaetzlich hinter der
#      Basic-Auth-Schicht (ENT-384).
#
#  (o) support_sammeln.php in die FilesMatch-Sperre aufgenommen
#      (2026-09-23). Der Abholweg des Betreibers: Was auf der eigenen
#      Datenbank eines Mandanten liegt -- Supportanfragen und der Stand der
#      Freigabe --, holt der Betreiber dort ab, statt dass der Mandant es
#      schickt. Wird per require_once aus backend/api/betreiber_support_vorgang.php
#      und backend/api/betreiber_support_lage.php gezogen und nie direkt
#      aufgerufen -- dieselbe Begruendung wie bei (e) und (f). Auch dieser
#      Hash ist dem manuellen Nachtrag vorausgeeilt, aus demselben Grund wie
#      (a), (c) bis (n): Ohne ihn waere die Regression dauerhaft rot und
#      nichts liesse sich mehr schieben. STEHT AUS. Die Datei enthaelt
#      ausschliesslich Funktionsdefinitionen und gibt bei direktem Aufruf
#      nichts aus; Staging liegt zusaetzlich hinter der
#      Basic-Auth-Schicht (ENT-384).
#
# Was das heisst: Fuer diese Aenderungen haelt der Drift-Guard den
# Staging-Deploy NICHT an. Production ist nicht betroffen -- dort laedt der
# Deploy die .htaccess selbst hoch. In Staging schuetzt bis zum Nachtrag die
# Basic-Auth-Schicht (ENT-384), nicht die FilesMatch-Sperre.
#
# Zu tun: die vollstaendige FilesMatch-Zeile bei Hostpoint eintragen
# (Explorer -> www/staging -> Web-Einstellungen -> Passwortschutz, unterhalb
# von #@__HCP_END__@#). Danach diesen Absatz loeschen -- er ist der einzige
# Hinweis darauf, dass der Hash dem Nachtrag vorausgeeilt ist.

htaccess-hostpoint:27890913ba199f9510a57954099f3bf5b1d5ffbf4c9dfa800dc4afdf7e1b43d8
htaccess-staging-zusatz:7b6df04b9fd9decbeb68442a6080b3e39c384e31d59273e53f938549c8e26208
robots-staging.txt:e120f27381f482e2de05b11355c7d1818e8266d282cec8cf04bc8b33916e7583
