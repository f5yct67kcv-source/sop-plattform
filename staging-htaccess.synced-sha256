# Hashes der drei Staging-Quelldateien zum Zeitpunkt der letzten manuellen
# Synchronisierung bei Hostpoint (Drift-Guard, ENT-384/ENT-387). Der
# Staging-Deploy vergleicht diese Werte mit den aktuellen Hashes der drei
# Dateien und bricht bei jeder Abweichung ab -- siehe deploy-hostpoint.yml,
# Schritt "Umgebung waehlen und erforderliche Secrets pruefen".
#
# htaccess-hostpoint       -> unterhalb von "#@__HCP_END__@#" in die
#                             Staging-.htaccess (Passwortschutz-Block bleibt
#                             oberhalb unangetastet)
# htaccess-staging-zusatz  -> direkt im Anschluss daran, in derselben
#                             Staging-.htaccess (X-Robots-Tag, ENT-387)
# robots-staging.txt       -> als eigene Datei robots.txt in www/staging
#
# Aktualisieren nach jeder inhaltlichen Aenderung an einer der drei Dateien:
# 1. sha256sum htaccess-hostpoint htaccess-staging-zusatz robots-staging.txt
# 2. Die geaenderte Datei wie oben beschrieben erneut manuell bei Hostpoint
#    eintragen (Explorer -> www/staging).
# 3. Den neuen Hash unten in der passenden Zeile eintragen und committen.
# ── Stand 2026-09-23: synchron ─────────────────────────────────────────
# Die Staging-.htaccess ist bei Hostpoint auf genau den Stand der beiden
# Hashes unten gebracht (ganzer Block unterhalb von #@__HCP_END__@#,
# erzeugt mit staging-htaccess-bauen.py; Passwortschutz oberhalb
# unveraendert). Damit sind die bis dahin offenen Nachtraege (a) bis (q)
# auf einmal erledigt. Geprueft: ohne Anmeldung 401, neuerungen.php nach
# der Anmeldung 403. Kein Hash eilt mehr einem Nachtrag voraus.

# ── OFFEN seit dem Zusammenfuehren von ENT-686 (2026-09-23) ──────────
# EIN Nachtrag steht aus: mandant_vorrat in der FilesMatch-Sperre. Das
# Modul prueft den Mandanten-Vorrat und wird nur per require_once aus
# backend/api/betreiber_vorrat_pruefen.php gezogen, nie direkt aufgerufen.
# Der Hash unten gehoert zur zusammengefuehrten htaccess-hostpoint und ist
# diesem Nachtrag vorausgeeilt -- ohne ihn waere test_deploy.mjs dauerhaft
# rot. Die Datei enthaelt nur Funktionsdefinitionen und Konstanten und gibt
# bei direktem Aufruf nichts aus; Staging liegt zusaetzlich hinter der
# Basic-Auth-Schicht (ENT-384). Zu tun: den Block unterhalb von
# #@__HCP_END__@# neu erzeugen (staging-htaccess-bauen.py) und bei Hostpoint
# eintragen, danach diesen Absatz loeschen.
#
htaccess-hostpoint:e60b95a7faebfd32bb6320d5e846089ab694b1bf0e53241e2daf95c3e2db0149
htaccess-staging-zusatz:7b6df04b9fd9decbeb68442a6080b3e39c384e31d59273e53f938549c8e26208
robots-staging.txt:e120f27381f482e2de05b11355c7d1818e8266d282cec8cf04bc8b33916e7583
